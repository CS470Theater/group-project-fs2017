@model IEnumerable<TheaterBooking.Web.Areas.Admin.Models.Screen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Theater.Theater_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Capacity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Theater.Theater_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Screen_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Screen_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Screen_ID })
        </td>
    </tr>
}

</table>

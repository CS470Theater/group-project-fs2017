@model IEnumerable<TheaterBooking.Models.Rating>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rating_Symbol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rating_Symbol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Rating_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Rating_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Rating_ID })
        </td>
    </tr>
}

</table>
